1)
Можно передать: само значение, ссылку на переменную, указатель, объявить глобально переменную, объявить через define, через аргументы по умолчанию
2)
создаются до main, удаляются после main, хранятся в static => не удаляется при выходе из функции
3)
void function(type name,.../*ничего нового*/, type name = value,/*вот это и есть аргументы по умолчанию*/...){ ... }
4)
если у нас есть пара функций, одна из которых работает (к примеру) с double, а другая - с char и мы подаём переменную типа double(char), то работает первая(вторая) функция
если же подаём чтото другое, то компилятор смотрит, к какому типу можно привести полученное значение, например если введём int, то будет работать первая.
5)
/*А я ведь начинал записывать информацию про inline :(  */
на этапе препроцессинга компилятор берет макросы и подставляет в код, а inline не так работает
inline никто не мешает быть многострочной функцией, а для definе нужно (я так никогда не делал) ставить в конце каждой строки "\", чтобы перейти на следующую
различные области видимости переменных из макросов и inline



